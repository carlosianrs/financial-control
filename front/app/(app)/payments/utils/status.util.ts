export enum StatusPayment {
  pending = 'Pendente',
  paid = 'Pago',
  received = 'Recebido',
}

export enum StatusColor {
  pending = 'text-yellow-400',
  paid = 'text-red-400',
  received = 'text-green-400',
}